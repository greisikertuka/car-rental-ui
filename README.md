# Generate code for the frontend via openapi code generator
To install the code generator, run this:
```shell
npm install --save-dev @openapitools/openapi-generator-cli
```

To generate code, run htis:
```shell
npm run generate-api
```



